# Partial implementation continuation fixture

Continue this approved plan:

1. Add `formatName` in `src/name.ts`.
2. Add focused tests in `src/name.test.ts`.
3. Run `python test_runner.py`.

Current state:

- `src/name.ts` already contains the exact required `formatName` behavior.
- `src/name.test.ts` does not exist.
- The implementation of step 1 must be verified and not redone.
