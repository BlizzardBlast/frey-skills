# Accepted scorecards

Commit accepted compact behavioral scorecards for this skill in this directory.
Keep raw prompts, transcripts, disposable repositories, rejected runs, and
grading notes under ignored `eval-workspace/`; never infer missing trials.

Each scorecard must identify `implementation-execution`, contain one result for
every current eval ID, and follow `eval-scorecards/README.md`.
