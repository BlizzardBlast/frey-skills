interface:
  display_name: 'Implementation Execution'
  short_description: 'Execute approved plans with traceability'
  default_prompt: 'Use $implementation-execution to execute this approved plan through bounded edits and focused verification.'
policy:
  allow_implicit_invocation: true
