interface:
  display_name: 'Implementation Plan'
  short_description: 'Read-only repository-grounded planning'
  default_prompt: 'Use $implementation-plan to inspect the current codebase and produce an implementation-ready plan without editing files.'
policy:
  allow_implicit_invocation: true
