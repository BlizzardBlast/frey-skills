# Direct implementation non-trigger fixture

User request: implement the TODO now. Do not spend time producing an implementation plan.

```ts
export function formatAccount(accountNumber: string) {
  // TODO: group the account number in blocks of four.
  return accountNumber;
}
```

This is an implementation request, not a planning request.
