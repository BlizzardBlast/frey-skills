interface:
  display_name: 'Test Strategy'
  short_description: 'Risk-based test strategy and readiness'
  default_prompt: 'Use $test-strategy to create a repository-grounded, risk-based testing strategy for this change.'
policy:
  allow_implicit_invocation: true
