interface:
  display_name: 'Code Review'
  short_description: 'Read-only review with a decision'
  default_prompt: 'Use $code-review to review the current diff, produce a coverage matrix, and decide merge readiness.'
policy:
  allow_implicit_invocation: true
